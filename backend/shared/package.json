{
  "name": "@house-by-us/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types and constants — no runtime deps, pure TypeScript",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "peerDependencies": {
    "@house-by-us/drizzle": "workspace:*"
  }
}
