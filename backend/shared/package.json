{
  "name": "house-by-us",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["server", "shared", "drizzle"],
  "scripts": {
    "dev": "npm run dev --workspace=server",
    "build": "npm run build --workspace=server",
    "db:push": "npm run db:push --workspace=drizzle",
    "db:studio": "npm run db:studio --workspace=drizzle"
  }
}
